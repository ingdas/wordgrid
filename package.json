{
  "name": "wordgrid",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A category-finding word puzzle where four groups all share one pivot word.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "submission": "node scripts/gen-submission.mjs",
    "clip": "node scripts/gen-clip.mjs",
    "validate": "node --experimental-strip-types scripts/validate.mts",
    "audit": "node --experimental-strip-types scripts/audit.mts",
    "test": "node --experimental-strip-types scripts/engine.test.mts && node --experimental-strip-types scripts/progress.test.mts && node --experimental-strip-types scripts/quests.test.mts && node --experimental-strip-types scripts/storage.test.mts && node --experimental-strip-types scripts/debug.test.mts && node --experimental-strip-types scripts/i18n.test.mts && node --experimental-strip-types scripts/sdk.test.mts && node --experimental-strip-types scripts/stats.test.mts && node --experimental-strip-types scripts/audio.test.mts"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.2.9",
    "@fontsource-variable/inter": "^5.2.8",
    "gsap": "^3.15.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^26.3.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "puppeteer-core": "^25.3.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
