node_modules
dist
*.local
.DS_Store
.vite

# NOTE: /docs is intentionally committed — it is the GitHub Pages build.

*.tsbuildinfo

# The stats server's SQLite file, if it is run from the repo root.
stats.db
stats.db-*
